plugins {
    id 'fabric-loom' version '1.7-SNAPSHOT'
    id 'maven-publish'
}

version = project.mod_version
group = project.maven_group

base {
    archivesName = project.archives_base_name
}

repositories {
    // Репозитории для зависимостей, если понадобятся (например, для библиотек меню)
}

dependencies {
    // Основные зависимости для разработки
    minecraft "com.mojang:minecraft:1.21.1"
    mappings loreli.officialMojangMappings() // Официальные маппинги от Mojang
    modImplementation "net.fabricmc:fabric-loader:${project.loader_version}"

    // Подключаем Fabric API (нужен для событий удара и команд)
    modImplementation "net.fabricmc.fabric-api:fabric-api:${project.fabric_version}"
}

processResources {
    inputs.property "version", project.version

    filesMatching("fabric.mod.json") {
        expand "version": project.version
    }
}

tasks.withType(JavaCompile).configureEach {
    it.options.release = 21
}

java {
    withSourcesJar()

    sourceCompatibility = JavaVersion.VERSION_21
    targetCompatibility = JavaVersion.VERSION_21
}

jar {
    from("LICENSE") {
        rename { "${it}_${project.archivesBaseName}" }
    }
}